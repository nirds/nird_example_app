<%= form_for(@gif) do |f| %>
  <% if @gif.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gif.errors.count, "error") %> prohibited this gif from being saved:</h2>

      <ul>
      <% @gif.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :feeling %><br>
    <%= f.text_field :feeling %>
  </div>
  <div class="field">
    <%= f.label :gif_id %><br>
    <%= f.text_field :gif_id, :value => @gif.gif_id  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
